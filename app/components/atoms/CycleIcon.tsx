import AutorenewIcon from '@mui/icons-material/Autorenew';

/**
 * Re-export of MUI AutorenewIcon — used throughout CI Planning to represent cycles/iterations.
 * Accepts all MUI SvgIconProps (fontSize, color, sx, etc.).
 */
const CycleIcon = AutorenewIcon;

export default CycleIcon;
